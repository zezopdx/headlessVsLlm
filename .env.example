# LLM — Heroku Managed Inference (enterprise-billed; no personal token)
# Pull these from the Heroku app, e.g.:
#   heroku config:get INFERENCE_KEY -a <your-app>
#   heroku config:get INFERENCE_URL -a <your-app>
#   heroku config:get INFERENCE_MODEL_ID -a <your-app>
INFERENCE_KEY=your_heroku_inference_key
INFERENCE_URL=https://us.inference.heroku.com
INFERENCE_MODEL_ID=claude-4-5-sonnet

# Snowflake
SNOWFLAKE_ACCOUNT=your_account_identifier
SNOWFLAKE_USER=your_username
SNOWFLAKE_PRIVATE_KEY_FILE=snowflake_rsa_key.p8   # local: path to .p8 file
SNOWFLAKE_PRIVATE_KEY_B64=                         # Heroku: base64-encoded .p8 contents (heroku config:set SNOWFLAKE_PRIVATE_KEY_B64="$(base64 -i snowflake_rsa_key.p8)")
SNOWFLAKE_WAREHOUSE=your_warehouse
SNOWFLAKE_ROLE=your_role
# No default database/schema — queries use fully qualified names (DB.SCHEMA.TABLE)
